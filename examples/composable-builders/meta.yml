id: composable-builders
title: Composable Builders
description: |
  Shows how to create reusable option builders that can be shared across
  commands. This pattern reduces duplication when multiple commands need
  the same options like verbosity, output format, or configuration paths.
test:
  - name: "Build with Options"
    options:
      command: 'npx tsx --no-cache --tsconfig ../tsconfig.json cli.ts build --verbose --format json'
    assertions:
      stdout:
        matches: 'verbose: true.*format: json'

  - name: "Serve with Options"
    options:
      command: 'npx tsx --no-cache --tsconfig ../tsconfig.json cli.ts serve --port 8080 --verbose'
    assertions:
      stdout:
        matches: 'verbose: true.*port: 8080'
