id: circular-config-detection
title: Circular Config Detection
test:
  - name: "Circular config detection error"
    options:
      command: 'npx tsx --no-cache --tsconfig ./examples/tsconfig.json examples/circular-config-detection/cli.ts'
    assertions:
      exitCode: 1
      stderr:
        contains: 'Circular reference detected'
